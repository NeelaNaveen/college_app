<%= form_for(@registration_room) do |f| %>
  <% if @registration_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registration_room.errors.count, "error") %> prohibited this registration_room from being saved:</h2>

      <ul>
      <% @registration_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :students_id %><br>
    <%= f.text_field :students_id %>
  </div>
  <div class="field">
    <%= f.label :courses_id %><br>
    <%= f.text_field :courses_id %>
  </div>
  <div class="field">
    <%= f.label :semester %><br>
    <%= f.number_field :semester %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
